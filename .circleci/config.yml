version: 2.0

jobs:
  build:
    docker:
      - image: circleci/ruby
    steps:
      - checkout
      - run: |
          sudo apt-get install cppcheck
          gem install pdd
          cmake .
          ./style.sh
          make VERBOSE=1
          make test
          ./test_report.py -o old-test.xml ./test_goost
      - store_test_results:
          path: .
