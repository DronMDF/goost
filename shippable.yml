language: c

compiler:
  - gcc
  - clang

build:
  ci:
    - apt-get update
    - gem install pdd
    - ./style.sh
    - apt-get install cmake
    - cmake .
    - make VERBOSE=1
    - ./test_report.py -o shippable/testresults/result.xml ./test_runner
