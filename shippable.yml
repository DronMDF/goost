language: c

# TODO: Very old ubuntu image 14.04, clang is not work with c++11
compiler:
  - gcc

build:
  ci:
    - apt-get update
    - apt-get install cmake
    - cmake .
    - make
    - ./test_runner -t
