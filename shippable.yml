language: c

compiler:
  - gcc
  - clang

build:
  ci:
    - apt-get update
    - apt-get install cmake cppcheck ruby-dev
    - pip install cpplint
    - gem install rake pdd
    - cmake .
    - make style
    - make VERBOSE=1
    - make test
