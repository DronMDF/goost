language: c

compiler:
  - gcc
  - clang

build:
  ci:
    - apt-get update
    - apt-get install cmake ruby-dev
    - gem install pdd
    - ./style.sh
    - cmake .
    - make VERBOSE=1
    - ./test_report.py -o shippable/testresults/result.xml ./test_runner
